apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: store-front
spec:
  scaleTargetRef:
    name: store-front
  minReplicaCount: 1
  maxReplicaCount: 100
  triggers:
  - type: cpu
    metricType: Utilization
    metadata:
      value: "50"